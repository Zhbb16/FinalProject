<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item android:id="@+id/home"
        android:orderInCategory="50"
        app:showAsAction="never"
        android:title="@string/home_menu"/>    <!-- This uses showAsAction=always, so look at icon= parameter -->

    <item android:id="@+id/favorite"
        android:title="@string/favorite_menu"
        app:showAsAction="never"
        android:orderInCategory="100"/> <!-- This uses showAsAction=never, so look at title= parameter -->

    <item android:id="@+id/exit"
        android:title="@string/exit_menu"
        app:showAsAction="never"
        android:orderInCategory="150"/>

</menu>